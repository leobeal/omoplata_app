# User Identifications

The `user_identifications` table stores external identifiers for users such as
access card IDs, standalone card numbers, or QR codes. Each record contains
the type of identification and the corresponding code, allowing a user to have
multiple methods of identification.
