# Failed Payments Ratio

A Livewire component that shows the percentage of failed payments out of all processed payments for the current month. The ratio is displayed as a simple value on the admin dashboard.

## Usage

```blade
<livewire:app.dashboard.failed-payments-ratio />
```
